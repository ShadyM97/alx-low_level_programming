#include<stdlib.h>
#include<time.h>
#include<stdio.h>

/**
 * main - Positive or Negative
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alphabet;
	for( alphabet = 97; alphabet < 124; alphabet++)
		putchar(alphabet);

	return (0);
}
